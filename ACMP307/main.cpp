#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n,m,p;
    int ans = 0;
    cin >> n >> m >> p;






//    vector<int> a(p);
//    vector<int> b(p);
//    for (int i = 0;i < p;i++){
//        cin >> a[i] >> b [i];
//    }
//
//    for (int i = 1;i < p;i++){
//        for (int j = 0;j < i;j++){
//            if ((a[i] < a[j] && b[i] > b[j]) || (a[i] > a[j] && b[i] < b[j]))
//                ans++;
//        }
//    }
    cout << ans << endl;
    return 0;
}