#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--){
        int n;
        vector<string> p(n);
        cin >> n;
        for (int i = 0;i < n;i++)
            cin >> p[i];


    }
    return 0;
}