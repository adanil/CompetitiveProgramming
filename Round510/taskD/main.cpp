#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
using ll = long long;

int main() {
    ll n,t;
    cin >> n >> t;
    vector<ll>arr(n);
    ll sum = 0;
    for (int i = 0;i < n;i++){
        ll a;
        cin >> arr[i];
    }

    return 0;
}