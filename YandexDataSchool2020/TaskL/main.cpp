#include <iostream>
#include <vector>


using namespace std;

int main() {
    int w,h,a,b;
    cin >> w >> h >> a >> b;
    int n;
    cin >> n;
    for (int i = 0;i < n;i++){
        int xld,yld,xru,yru;
        cin >> xld >> yld >> xru >> yru;

    }

    return 0;
}